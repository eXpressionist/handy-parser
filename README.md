# Handy Parser

Лёгкий мониторинг цен и других значений на веб-страницах с уведомлениями в Telegram.

**Статус: ранняя alpha.** Рабочий первый срез уже реализован: HTML/CSS и PSP-парсинг, SQLite, правила изменения цены, Telegram outbox, защищённая веб-панель, разовая команда проверки и Dockerfile. До первого стабильного релиза остаются проверка Docker на Debian 13, замеры на VPS, расширение тестов и публикация готового образа в GHCR.

## Задача

- Debian 13, VPS с 2 CPU и 2 ГБ RAM, около 800 МБ свободно; на сервере уже работают другие приложения.
- До 10 наблюдений, четыре проверки в сутки, возможность запуска вручную.
- URL и CSS-селекторы можно вводить вручную; для PSP предусмотрен встроенный обработчик API.
- Небольшая веб-панель: настройка, предпросмотр значения, статус и история.
- Telegram: старое и новое значение, ссылка; отдельные уведомления о длительной недоступности источника.
- Установка через Docker Compose, хранение данных вне контейнера.

## Выбранный подход

Go + SQLite. Один бинарный файл, два основных режима: `serve` для веб-панели и `check` для разовой проверки. Веб-панель работает отдельным небольшим контейнером. Таймер systemd на хосте четыре раза в сутки запускает временный контейнер `check`; после проверки он завершается и удаляется. Между проверками процесс парсера отсутствует. Docker Engine и веб-панель продолжают занимать память.

Chromium, Redis, отдельный сервер БД и постоянно работающий Telegram-бот в первой версии не требуются. Изменение наблюдения и кнопка ручной проверки доступны в панели. Ручная проверка использует то же ядро проверки и блокировку, что и команда `check`.

## Документация

| Документ | Содержание |
| --- | --- |
| [Архитектура и требования](docs/ARCHITECTURE.md) | Компоненты, данные, обработка ошибок, ресурсные цели |
| [Пошаговый план](docs/IMPLEMENTATION_PLAN.md) | Этапы разработки и критерии готовности |
| [Установка и эксплуатация](docs/INSTALLATION.md) | Docker, секреты, расписание, обновления, резервное копирование |
| [Публикация и релизы](docs/RELEASING.md) | GitHub, CI, образы GHCR, выпуск версии |
| [Исследование сайтов](research/README.md) | Проверенные HTML/API-источники GPC и PSP |
| [Шаблоны развёртывания](deploy/) | Примеры Compose, окружения и systemd |

## Проверенные источники

| Источник | Метод | Результат проверки 16.09.2026 |
| --- | --- | --- |
| GPC | HTTP GET, `meta[name="product:price:amount"]`, атрибут `content` | 137.95 GEL, совпало с браузером |
| PSP | Публичный GraphQL: `urlResolver` → `productsByID` | 89.67 GEL, обычная цена 137.95 GEL; совпало с браузером |

Это снимок результатов с компьютера разработчика, а не обещание текущей цены. Доступность с IP VPS необходимо проверить перед первым релизом.

## Локальный запуск для разработки

Требуется Go 1.27:

```sh
go test ./...
go build -o handy-parser ./cmd/handy-parser
HANDY_ADMIN_PASSWORD='replace-me' ./handy-parser serve
```

По умолчанию база создаётся в `data/handy.db`, панель доступна на `127.0.0.1:8080`. Для Docker нужны файлы `secrets/admin_password` и `secrets/telegram_bot_token`; полный порядок описан в инструкции установки.

## Порядок работы

1. Завершить автоматические проверки runner, Telegram и веб-панели.
2. Собрать Docker-образ и проверить контейнеры с таймером на Debian 13.
3. Измерить память на целевом VPS.
4. Выпустить первый тег с образом в GHCR.

Подробные выполненные и оставшиеся задачи перечислены в [плане реализации](docs/IMPLEMENTATION_PLAN.md). Alpha-код пригоден для разработки и тестирования; установку на рабочий VPS лучше выполнять после первого помеченного релиза.
