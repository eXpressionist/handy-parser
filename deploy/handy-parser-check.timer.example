[Unit]
Description=Check monitored pages four times a day

[Timer]
# Suggested schedule; change the timezone explicitly if needed.
OnCalendar=*-*-* 00,06,12,18:00:00 Europe/Moscow
Persistent=true
AccuracySec=1min
Unit=handy-parser-check.service

[Install]
WantedBy=timers.target
